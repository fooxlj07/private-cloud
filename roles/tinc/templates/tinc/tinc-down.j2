#!/bin/sh
#
# Remove IP and routing. IP must be from /etc/tinc/{{ vpn_name }}/hosts/server*
# 
#!/bin/bash

ip addr del {{ priv_ip }}/{{ subnet_mask }} dev {{ vpn_iface }}
ip link set {{ vpn_iface }} down
ip addr flush dev {{ vpn_iface }}
